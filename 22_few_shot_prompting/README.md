# 22 — Few-Shot Prompting

Teach LLMs by example using static and dynamically-selected few-shot prompts.

## What You'll Learn

- Build static few-shot prompt templates
- Select examples dynamically using semantic similarity
- Combine few-shot prompting with structured output

## Run

```bash
export OPENAI_API_KEY="your-key"
python main.py
```
