# 15 — Hybrid Search (Vector + Keyword)

Combine vector similarity search with BM25 keyword search using Reciprocal Rank Fusion.

## What You'll Learn

- Run parallel vector and keyword retrievals
- Merge ranked lists with Reciprocal Rank Fusion (RRF)
- Use fused results for higher-quality RAG answers

## Run

```bash
export OPENAI_API_KEY="your-key"
python main.py
```
