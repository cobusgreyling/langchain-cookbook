# 07 — Memory Patterns

Explore different memory strategies for conversational AI: buffer, summary, and entity memory.

## What You'll Learn

- Simple message buffer memory
- Sliding window memory (last N messages)
- Summary memory that compresses conversation history
- How memory affects context and costs

## Run

```bash
export OPENAI_API_KEY="your-key"
python main.py
```
