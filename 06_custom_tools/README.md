# 06 — Custom Tool Creation

Create custom tools that an LLM agent can use — from simple functions to tools with complex inputs.

## What You'll Learn

- Define tools with the `@tool` decorator
- Create tools with structured (multi-parameter) inputs
- Build a tool-using agent

## Run

```bash
export OPENAI_API_KEY="your-key"
python main.py
```
