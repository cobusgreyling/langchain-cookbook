# 21 — Persistent Chat Memory

Store conversation history in SQLite for persistence across sessions.

## What You'll Learn

- Build a SQLite-backed message store
- Manage multiple chat sessions independently
- Resume conversations with full history recall

## Run

```bash
export OPENAI_API_KEY="your-key"
python main.py
```
