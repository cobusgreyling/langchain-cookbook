# 05 — Streaming Responses

Stream LLM responses token-by-token for responsive UIs and real-time output.

## What You'll Learn

- Stream tokens from an LLM
- Stream through a chain (prompt + LLM + parser)
- Stream events from an agent with tools

## Run

```bash
export OPENAI_API_KEY="your-key"
python main.py
```
