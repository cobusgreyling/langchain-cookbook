# 02 — Conversational Agent with Tool Use

Build an agent that can use tools (calculator, web search simulation) while maintaining conversation context.

## What You'll Learn

- Define custom tools for an agent
- Use the ReAct agent pattern
- Maintain conversation history

## Run

```bash
export OPENAI_API_KEY="your-key"
python main.py
```
